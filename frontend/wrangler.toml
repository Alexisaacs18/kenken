name = "kenken-frontend"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[build]
command = "npm run build"
cwd = "."

[env.production]
name = "kenken-frontend"

# Note: For Cloudflare Pages, service bindings are configured in the dashboard
# If you need to bind to another Worker, add it here:
# [[services]]
# binding = "WORKER_NAME"
# service = "your-worker-name"
